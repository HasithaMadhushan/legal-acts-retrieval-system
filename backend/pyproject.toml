[project]
name = "legal-acts-retrieval-backend"
version = "0.1.0"
description = "FastAPI backend for an academic Sri Lankan Legal Acts retrieval MVP"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["app/tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]
